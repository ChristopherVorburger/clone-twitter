import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
  notifications__link_nav: {
    textTransform: "none!important",
    padding: "0 2rem!important",
    minHeight: "53px!important",
  },
}));
